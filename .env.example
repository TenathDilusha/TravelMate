# ---- Database (Backend) ----
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=travelmate
DB_PORT=5432

# On Render, set DATABASE_URL instead (auto-provided by linked PostgreSQL)
# DATABASE_URL=

# ---- Frontend ----
VITE_API_BASE_URL=http://localhost:8000
